SyncCraft

SyncCraft is an offline-first collaborative text editor built to explore CRDTs (Conflict-free Replicated Data Types), real-time synchronization, and distributed systems.

The goal is to allow multiple users to edit the same document, even when they temporarily lose their internet connection, and automatically merge their changes when they reconnect.